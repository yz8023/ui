# 协议与开源许可页面 · 环境要求

## 依赖

- Jetpack Compose Material 3
- 本项目 `GlassDialog`
- 设置页 `PreferenceRow` / `PreferenceCard` 可选

## 需要的文件

```text
ui/features/LegalInfoDialog.kt
ui/components/Dialogs.kt
```

如果不用玻璃弹窗，可以把 `GlassDialog` 换成 Material3 `AlertDialog`。
